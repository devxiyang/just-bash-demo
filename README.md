# just-bash-demo

仓库现在拆成两个顶层目录：

- `code/`：可运行的 just-bash demo、HTML presentation 和本地服务
- `presentation/`：演讲相关材料，包括草稿、旧版 deck 和预览图

运行 demo：

```bash
cd code
npm install
npm run web
```

主要入口：

- `code/public/presentation.html`
- `code/public/index.html`
